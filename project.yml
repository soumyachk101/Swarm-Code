name: DroppyCode
options:
  bundleIdPrefix: iordv
  deploymentTarget:
    macOS: "26.0"
  developmentLanguage: en
packages:
  SwiftTerm:
    url: https://github.com/migueldeicaza/SwiftTerm
    exactVersion: 1.20.0
settings:
  base:
    MARKETING_VERSION: "1.5.2"
    CURRENT_PROJECT_VERSION: "14"
    ARCHS: arm64
    EXCLUDED_ARCHS: x86_64
    SWIFT_VERSION: "6.0"
    SWIFT_APPROACHABLE_CONCURRENCY: YES
    DEVELOPMENT_TEAM: NARHG44L48
    MACOSX_DEPLOYMENT_TARGET: "26.0"
targets:
  DroppyCode:
    type: application
    platform: macOS
    sources:
      - path: DroppyCode
        type: syncedFolder
      # The license, the third-party notices and the trademark policy travel with every copy of
      # the app; About shows them under Licenses.
      - path: LICENSE
        buildPhase: resources
      - path: THIRD_PARTY_NOTICES.md
        buildPhase: resources
      - path: TRADEMARK.md
        buildPhase: resources
    dependencies:
      - package: SwiftTerm
    settings:
      base:
        PRODUCT_NAME: "Droppy Code"
        PRODUCT_MODULE_NAME: DroppyCode
        PRODUCT_BUNDLE_IDENTIFIER: iordv.droppycode
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: "Droppy Code"
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.developer-tools
        INFOPLIST_KEY_NSHumanReadableCopyright: "Droppy Code, the coding app by Droppy. MIT License."
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ENABLE_HARDENED_RUNTIME: YES
        CODE_SIGN_STYLE: Automatic
        DEAD_CODE_STRIPPING: YES
        SWIFT_OBJC_BRIDGING_HEADER: DroppyCode/Support/DroppyCode-Bridging-Header.h
      configs:
        Debug:
          PRODUCT_NAME: "Droppy Code Dev"
          PRODUCT_BUNDLE_IDENTIFIER: iordv.droppycode.dev
          INFOPLIST_KEY_CFBundleDisplayName: "Droppy Code Dev"
        Release:
          CODE_SIGN_STYLE: Manual
          CODE_SIGN_IDENTITY: Developer ID Application
