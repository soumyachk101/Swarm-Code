# Swarm Code audit, 15 September 2026

A read of the whole app (99 Swift files, ~36k lines) plus measurements on this Mac: the real
library (47 threads, 2 projects, 31 heads), the attachment folder, the checkout's git
objects and the provider CLIs. Findings are grouped by area and ranked P0 (fix now) to P3
(nice to have). The ones marked **fixed** are in this branch; everything else is a
recommendation with the file and line it points at.

## P0: the checkout did not build

`SwarmCode/Views/Chat/ChatView.swift` in the lead's checkout held three unresolved
three-way merge hunks (`<<<<<<< ours` … `>>>>>>> theirs` at the old lines 160, 206 and 247),
left behind when a head's landing (`Git.apply` with `--3way`) could not settle the panel
refactor against the checkout. The "ours" side referenced `origin`, `panelDrag` and
`layout.panelHeight`, none of which exist any more; the "theirs" side is the `PlacedPanel`
call the rest of the file already uses. **Fixed**: the three hunks resolve to the
`PlacedPanel` version. A Debug build then succeeds with no new warnings.

This is also the clearest example of the Hydra landing hazard in the next section: the
markers sat in a file nobody was told to look at, and with "Merge when the team is done" on
they would have been committed and pushed.

## Hydra

| # | Finding | Where | Priority |
|---|---|---|---|
| H1 | **Auto-merge could push conflict markers.** `autoMergeHydraWork` committed every touched path as-is; a landing that left markers (`HydraLanding.conflicts`) went to the remote if the lead answered without settling it. **Fixed**: the merge now scans the team's files for `<<<<<<< ` / `>>>>>>> ` lines off the main actor and, finding any, leaves a Hydra note naming them instead of merging. | `App/AppModel+HydraMerge.swift:43` | P0 |
| H2 | **Landings happen while the lead is mid-turn.** A completed head's patch is applied to the lead's checkout the moment the head finishes (`hydraHeadTurnFinished` → `landHydraHead`), even when the lead is still editing. If the lead's current turn has already changed one of the patched files, `git apply --3way` is the thing that writes markers into a file the lead is working on. Hold a landing whose paths intersect the lead's `touchedPaths` for the running turn until `phase == .idle`, and land the rest at once. | `App/AppModel+Hydra.swift:303`, `Runtime/ThreadRuntime.swift:1146` | P1 |
| H3 | **Conflicts are nearly invisible to the user.** After a conflicted landing the only signs are "landed 3 files, 1 conflict" in the panel row and a line in the report the lead reads. Add a warning `Notice` row to the lead's timeline naming the files (`appendNotice(.warning, …)` is already there), and show the count on the Hydra button badge. | `App/AppModel+Hydra.swift:342`, `Views/Chat/HydraPanel.swift:529` | P1 |
| H4 | **Dangling copy path with "Clear finished heads automatically".** `finishHydraHead` releases the head's worktree (`releaseHydraCopy`) before the report is relayed with `copyPath`, so a stopped or failed head's report points the lead at a folder that was just deleted. Release after `hydraHeadFinished`, or drop `copyPath` from the report when auto-clear is on. | `App/AppModel+Hydra.swift:263` and `:273` | P2 |
| H5 | **Landing patches are parsed on the main actor.** `DiffParser.parse(patch)` runs inside `applyHydraHead` (a `--binary` patch can be large); use the `@concurrent` parse the runtime already has (`ThreadRuntime.parseDiff`). | `App/AppModel+Hydra.swift:340` | P2 |
| H6 | **The charge ring animated forever.** With Hydra on, every chat's chrome row spun the roster ring and breathed the glow all day, inside a Liquid Glass circle that re-renders on every frame its content moves. **Fixed**: the ring and glow move only while a head is working; otherwise the ring stands still (reduced motion keeps it still too). | `Views/Chat/HydraButton.swift` | P1 |
| H7 | The Hydra panel is a glass surface floating over a streaming transcript, showing a second streaming transcript inside it: two live timelines plus a glass resample per token. While either transcript streams, swap the panel's glass for the scrim the morph ghost already uses (`HydraPanelGhost`). | `Views/Chat/HydraPanel.swift:96` | P2 |
| H8 | `reportMessage` writes the section heads as `## Hank — task` with an em dash, the mark the app strips from every model reply; the lead reads it and may echo it. Use ` - ` or `:`. | `Models/Hydra.swift:666` | P3 |
| H9 | The 25 personas keep going as "Hank 2" past the roster; fine, but `hydraSpawnCount` is never reset for a chat, so a long-lived lead's heads drift into second-round names while the panel shows only a few. Cosmetic. | `App/AppModel+Hydra.swift:140` | P3 |

## Chat, sending and the blank pane

The lead is on the blank-pane bug; these are the two structural causes I would test first,
both in `Views/Chat/ThreadTimeline.swift`.

| # | Finding | Where | Priority |
|---|---|---|---|
| C1 | **The window is a suffix of fixed length, so every appended block evicts one at the top.** `hidden = blocks.count - visibleCount` (line 90): sending a message appends two blocks (the prompt and the working line) and removes the two oldest rows from the lazy stack in the same pass, while the bottom anchor tries to hold. Track `hiddenCount` instead of `visibleCount` while a turn runs (freeze how many older blocks stay hidden), so appends never change which older rows exist. | `ThreadTimeline.swift:90`, `:413` | P1 |
| C2 | **The turn fold swaps N identities for one.** A finished turn's user row, work groups, reply rows and working line (ids `entry.id` / `work-…` / `working-…`) all leave the `ForEach` and one `turn-<uuid>` block arrives (line 818). The lazy stack drops every measured height at once; the repair machinery (`armBlankWatch` / `repairLayout` / `stackGeneration`) exists to cover the aftermath. Give the running turn the same block id as the finished one (`turn-<uuid>` from the first user row, with the block's content switching from live rows to the collapsed summary), and the fold becomes an in-place content change. | `ThreadTimeline.swift:766`, `:818` | P1 |
| C3 | **Sending waits on git before the model hears anything.** `startTurn` runs `isRepository` and `captureCheckpoint` (four git processes) and only then `ensureSession` and `send`. Run the checkpoint with `async let` alongside `ensureSession`, await both before `send`: a cold session start (1–3 s) then overlaps the snapshot. | `Runtime/ThreadRuntime.swift:573`–`:583` | P2 |
| C4 | **Turn-end diff parsing on the main actor.** `finishTurn` parses the turn's patch (and the provider diff, twice) synchronously; `settleCommand` parses every command's patch in a main-actor `Task`. Use `Self.parseDiff` (`@concurrent`) for all of them. A large patch here is a hitch exactly when the reply lands. | `ThreadRuntime.swift:1136`, `:1140`, `:1152`, `:1649` | P1 |
| C5 | Sending a photo reads the file and base64-encodes it on the main actor at full size (a Retina screenshot is 5–10 MB, 13 MB as base64 through the pipe). Downscale once at import (2000 px longest side, JPEG) off the main actor; every provider and every token count benefits. | `Providers/ClaudeSession.swift:156`, `ACPSession.swift:127`, `DeepSeekSession.swift:802`, `MetaSession.swift:824` | P2 |
| C6 | `Write` tool calls store the whole written file as a `FileEdit` diff in the timeline and in the thread file, uncapped (command-derived edits are capped at 200 KB at `:1678`). Cap provider edits the same way. | `Providers/ClaudeSession.swift:583`, `Runtime/ThreadRuntime.swift:1678` | P2 |
| C7 | The debug `StripLog` logger still writes attachment names at notice level to the unified log on every thumbnail tap and removal. Delete it. | `Views/Common/AttachmentPreviewPanel.swift:8`, `Views/Chat/TimelineRows.swift:256`, `Views/Composer/ComposerView.swift:532`, `Views/Composer/FollowUpQueueTab.swift:449` | P2 |

## CPU, GPU and energy

| # | Finding | Where | Priority |
|---|---|---|---|
| E1 | **The whole window is one Liquid Glass surface** (`WindowBackdrop`), with a glass veil across the top of the pane while scrolled, glass capsules in the chrome row (one container, good), a glass composer and glass floating panels. Glass samples whatever is behind it every frame that changes, so a streaming reply keeps the veil, the panels and the composer re-sampling. When `backdropOpacity == 1` the backdrop still renders glass under a solid scrim: skip the effect there. Consider a behind-window `NSVisualEffectView` for the static backdrop; it is far cheaper than Liquid Glass and looks the same under the scrim. | `Views/Chrome/Chrome.swift:204`, `:500` | P1 |
| E2 | Hydra charge ring: fixed, see H6. | | |
| E3 | **Every save rewrites the whole thread file.** `saveNow` encodes all items and writes the file atomically; it runs 800 ms after every tool event, so a tool-heavy turn rewrites the file about once per tool call. The largest thread here is 1.6 MB: 300 tool calls ≈ 480 MB written per turn, plus 300 encodes of 1.6 MB on the disk-writer actor. Save at most every 5 s while a turn runs (always at turn end), or append events to a log and compact at turn end. | `Runtime/ThreadRuntime.swift:1721`, `:1730` | P1 |
| E4 | Two `git add -A` snapshots per shell tool call (`watchCommand` / `settleCommand`) to attribute edits to command rows. 25 ms on this repo (225 files), but it scales with the working tree: a monorepo pays seconds per command. Skip the snapshot for commands that cannot write (`ls`, `cat`, `grep`, `rg`, `find`, `git log|status|diff|show`, `head`, `tail`, `wc`, `sed -n`), which is most of what agents run. | `ThreadRuntime.swift:1639` | P2 |
| E5 | `Git.executable` resolves `git` with a PATH scan (`isExecutableFile` per entry) on every git call. Cache it once. | `Git/Git.swift:27` | P3 |
| E6 | Launch probes every installed CLI twice (`--version` and an auth probe): 16 processes at once with eight CLIs installed. Measured cheap here (all under 0.6 s each), so P3: stagger them, or probe auth lazily when a provider is first used or the Providers page opens. | `Providers/ProviderRegistry.swift:163` | P3 |

## Memory and disk

| # | Finding | Where | Priority |
|---|---|---|---|
| M1 | **Attachments were never cleaned up.** The folder held 315 files, 363 MB, of which 41 were referenced by any thread. **Fixed**: `Storage.sweepOrphanedAttachments` runs at the end of `bootstrap`, off the main actor: it scans the thread files as text for `attachments/<uuid>.<ext>`, and deletes only UUID-named files no thread references that are at least a day old (a draft open across a relaunch keeps its files). Dry run against the real folder: 41 referenced, 110 would go now, 164 wait a day. | `Store/Storage.swift`, `App/AppModel.swift:133` | P1 |
| M2 | A `ThreadRuntime` keeps its whole history in memory for the rest of the session; only the provider process is released after ten minutes idle. Opening every thread in a long session holds every history at once. Drop `entries` for a runtime idle for an hour with no session and reload from disk (the prefetch already decodes off-main), keeping `draft`, `followUps` and the panel state. | `App/AppModel.swift:295`, `Runtime/ThreadRuntime.swift:258` | P2 |
| M3 | Codex history fingerprints (one entry per session file) are kept in `UserDefaults`; thousands of rollouts make the preferences plist large and every defaults write slower. Keep them in a JSON file under Application Support. | `Views/Settings/TokenActivitySection.swift:75` | P3 |
| M4 | Turn checkpoints add little here (about 1 MB across 199 refs, delta-compressed), and `deleteCheckpoints` removes them with the thread. No action. | | |

## Startup

| # | Finding | Where | Priority |
|---|---|---|---|
| S1 | The library (100 KB) decodes on the main thread in `AppModel.init`; the em-dash cleaning of every opened thread's text runs on the main thread in `ThreadRuntime.init`. Both small today; move the cleaning into `DocumentPrefetch` so a warmed thread arrives clean. | `Runtime/ThreadRuntime.swift:270` | P3 |
| S2 | `LoginEnvironment.load` runs the interactive login shell (`-l -i`) before any provider is probed; a slow shell config delays provider status by its whole startup. Try the non-interactive `-l -c` first and fall back to `-i` if PATH is still missing the CLIs. | `Support/Shell.swift:208` | P3 |
| S3 | `WebsiteCaptures` (900 lines, ScreenCaptureKit and AVFoundation) ships in every build for a `--website-captures` flag. Compile it under a build flag so release builds link less and launch checks fewer branches. | `Support/WebsiteCaptures.swift` | P3 |

## Security and privacy

| # | Finding | Where | Priority |
|---|---|---|---|
| P1 | **API keys were duplicated in plaintext.** `AppSettings` wrote every DeepSeek and Meta key to `UserDefaults` as well as the Keychain, so `~/Library/Preferences/iordv.swarmai.plist` held them in the clear. **Fixed**: `setAPIKey` now reports whether the Keychain took the key; the defaults copy is written only when it did not, is removed once the Keychain holds the key, and a copy left by earlier builds is removed at launch when the Keychain has the key. | `App/AppSettings.swift`, `Providers/DeepSeekKeychain.swift`, `Providers/MetaKeychain.swift` | P1 |
| P2 | **Every link host in a reply is sent to a third-party favicon service.** `FaviconCache` fetches `google.com/s2/favicons?domain=<host>` for each host a reply links to, including internal hostnames from tool output. Make favicons opt-in, or fetch `/favicon.ico` from the host itself and skip private hosts (`localhost`, `.local`, `.internal`, RFC 1918 addresses, single-label names). | `Views/Markdown/MarkdownView.swift:422` | P1 |
| P3 | `StripLog` (C7) writes attachment file names to the unified log with `privacy: .public`. | `Views/Common/AttachmentPreviewPanel.swift:8` | P2 |
| P4 | The updater verifies the Developer ID team, the bundle identifier, the version and Gatekeeper before installing: good. The hardened runtime is on. No sandbox, which the CLIs need. | `App/AppUpdater.swift:237` | OK |

## Design and UX

| # | Finding | Where | Priority |
|---|---|---|---|
| D1 | Notices use the system `.orange` and `.red` while every other status colour goes through `Chrome.warning` / `Chrome.danger`; on a themed palette the notice row is the one thing off-hue. Same for the context meter's `.orange` past 85 %. | `Views/Chat/TimelineRows.swift:893`, `:901`, `Views/Composer/ComposerView.swift:464` | P2 |
| D2 | The sidebar's relative time ("3m") only refreshes when something re-renders the list; an idle app shows a stale age. A one-minute tick shared by the rows, or `Text(date, style: .relative)`, keeps it honest. | `Views/Sidebar/SidebarView.swift:893` | P3 |
| D3 | With "Every message goes to a head" on, the lead never hears the user's own words except through a head's report; the composer placeholder does not say so. Say "Goes to a head" in the placeholder and on the send button's help while the setting is on. | `Views/Composer/ComposerView.swift:240` | P3 |
| D4 | The chat re-renders on any thread's update anywhere: `ChatView.body` builds `PanelScene`, which filters the whole thread list (`subagent(of:)`, `hydraHeads(of:)`), and `HydraButton` does the same, so a title landing on another thread re-runs the chrome of this one. Keep the lead's head ids on the runtime (they are already tracked there for Swarm Code-run heads) and read the list through the per-thread cells. | `Views/Chat/ChatView.swift:278`, `Views/Chat/HydraButton.swift:19` | P2 |
| D5 | The sidebar builds its items with `helpers(of:)` per thread (a filter over every thread each time) and stores every row's measured height in `@State`, so each height report re-runs the whole list. Trivial at 47 threads, quadratic at hundreds: build one `helpersByParent` dictionary per pass and keep the heights in a non-observed holder like `FrameHolder`. | `Views/Sidebar/SidebarView.swift:188`, `:135` | P2 |
| D6 | Accessibility is well covered (labels, values, hints, reduced motion honoured everywhere that animates). Text sizes are fixed points throughout; there is no response to the system's larger-text preference. Fine for now. | | OK |
| D7 | All copy is English string literals with no localization tables; fine for a single-language app, worth knowing before adding any. | | P3 |

## Code health

| # | Finding | Where | Priority |
|---|---|---|---|
| K1 | 21 compiler warnings, all real: `RowGlide.swift` calls main-actor statics from the keyframe closure (make `smoothstep` and the insets `nonisolated`), `MarkdownView.swift:510` captures an `NSFont` in a `@Sendable` closure, `Popovers.swift:98` stores a closure in an `@Entry` (every update invalidates its readers; wrap it in an identity-comparable box), two `var` that never mutate, one deprecated `Text +`. | see the build log | P2 |
| K2 | `AppModel.setHydra(_:for:)` is dead ("legacy per-chat switch, now unused"). Remove it with `ChatThread.hydraEnabled` and `hydraPairID` once nothing decodes them. | `App/AppModel+Hydra.swift:46` | P3 |
| K3 | `handleLocalCommand` and `commandSuggestions` each list the providers that support `/compact`; put `supportsCompact` on `ProviderKind` beside `supportsRewind`. | `Runtime/ThreadRuntime.swift:505`, `Views/Composer/ComposerView.swift:379` | P3 |

## What this branch changes

- `SwarmCode/Views/Chat/ChatView.swift`: the three conflict hunks resolved to the `PlacedPanel` code.
- `SwarmCode/App/AppModel+HydraMerge.swift`: auto-merge refuses, with a note naming the files, while any of the team's files holds conflict markers (`pathsWithConflictMarkers`).
- `SwarmCode/Store/Storage.swift`, `SwarmCode/App/AppModel.swift`: `sweepOrphanedAttachments`, run at the end of `bootstrap`.
- `SwarmCode/App/AppSettings.swift`, `SwarmCode/Providers/DeepSeekKeychain.swift`, `SwarmCode/Providers/MetaKeychain.swift`: no plaintext key in the defaults while the Keychain holds it.
- `SwarmCode/Views/Chat/HydraButton.swift`: the charge moves only while a head works.
- `docs/audit-2026-09-15.md`: this document.

Checked with a Debug build in the worktree (`BUILD SUCCEEDED`, no new warnings in the files
touched) and a dry run of the sweep's rules against the real attachment folder. Nothing was
launched, and nothing was merged from here.

## Second pass, later on 15 September

A Hydra team (six workers, two scouts) worked through the findings above plus two audits of
its own, in one checkout with disjoint file ownership. Everything below builds with zero
warnings; none of it has been run live yet.

Fixed from the tables above: H2 (landings wait for an idle lead when the paths overlap the
running turn), H3 (a Hydra note names conflicted files), H4 (a stopped or failed head keeps
its copy until cleared, and its unlanded diff is kept as a patch before removal), H5, H8, H9,
C3, C4, C6, C7, E3 (one save per 5 s while a turn runs, with per-file write revisions so
saves never land out of order), E4 (no tree snapshots for read-only commands, one snapshot
chain per runtime), E5, E6, P2 (private hosts never reach the favicon service), D1, D2, D3,
D4 (per-parent child cells; the chat and the Hydra button no longer read the whole thread
list), D5, K1 (all 21 warnings), K3 not done, M2 and M3 (fingerprints moved to a JSON file),
S1 to S3 not done.

Hydra beyond the tables: the lead policy now says delegate first and that Hydra being on is
the user asking for heads; three delegation rounds; a wider head budget (35 min, 160
tools); one tree capture shared by a whole batch of heads; queued or sent heads report as
soon as the lead is idle instead of waiting behind a delegation batch; a delegation block
past 32 tasks leaves a note; a session that was replaced can no longer feed events into the
next turn; a stop pressed while a turn is starting takes effect at once; revert works on
every provider; the auto-merge moves the branch with an expected old value and refuses when
the checkout moved; `dirtyPaths` parses renames correctly.

The composer's model picker lists every pair on an installed provider above the models; a
tap enters the pair (lead model and effort, pinned, Hydra on) and the chip wears the mark.

Timeline: tool rows mount no AppKit anchor until hovered, allocate no popover until opened,
and hit the disk at most once per distinct image candidate; the working spinner is a CA
layer; display blocks are cached across body runs that change nothing in the conversation.
The Hydra button and panel no longer morph into each other; the panel fades in place.

Bug scout findings fixed: see the merge request description for the numbered list.

### The auto-merge itself

The first attempt to land this pass failed live: a file the lead had written outside the
checkout was among a turn's touched paths, and `git add -A -- <path>` refused it. The
same attempt would also have taken only the lead's own five files, because a native head's
edits arrive as agent events and were never part of the lead's touched paths. Fixed: paths
outside the repository never enter `touchedPaths` (`TouchedPaths.relative`); a native head's
reported edits and the tree diffs around its writing commands are folded into the lead's
turn (`hydraTouched`); the merge counts only turns not yet merged (`TurnRecord.hydraMerged`)
and unions the heads' own reported edits plus everything changed since the job's first
checkpoint that was not already dirty before it. For this job that is 56 files, not 5.
